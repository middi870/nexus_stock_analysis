# NEXUS v0.2.0 — Backend dependencies
# Pinned for reproducible Railway/Docker builds

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3

# Data layer
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.50      # pinned — 0.2.50+ has new content{} news schema

# HTTP client (intraday streaming, AI providers)
httpx==0.27.2
