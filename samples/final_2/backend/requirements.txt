# NEXUS — Backend dependencies
# Pinned for reproducible Railway builds

fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3

# Data
pandas==2.2.3
numpy==1.26.4
yfinance==0.2.50

# HTTP client (AI provider streaming)
httpx==0.27.2

# Optional — only needed for PostgreSQL (add DATABASE_URL to env)
# psycopg2-binary==2.9.10
# asyncpg==0.30.0
