# Copy to .env and fill in values you want to use.
# Ollama is used by default (no key needed).
# Cloud providers are optional — only needed if you switch in the AI sidebar.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
OPENROUTER_API_KEY=sk-or-...
